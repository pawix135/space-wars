{
  // Task to build the project using build.exe located in the workspace root.
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build with build.exe",
      "type": "shell",
      "command": "./build.exe",
      "args": [],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "presentation": {
        "reveal": "always",
        "panel": "shared",
        "close": false
      },
      "problemMatcher": "$gcc"
    },
    
  ]
}
